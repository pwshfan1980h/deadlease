# Current disposition of RULES_REVIEW.json

Implementation follow-up; this is not a new independent review. The original review and Hermes evidence are preserved.

- **RULES-001:** Existing fix preserved. Validation rejects a missing encounter in an undefeated hostile room while allowing previously defeated respawn rooms to remain idle. Covered by save corruption tests and real-command campaigns.
- **RULES-002:** Fixed. `SaveRepository` remembers exact content observed at successful load/write. Normal writes compare that token inside `StorageDriver.update`, which executes in the same native IndexedDB read/write transaction as the replacement. Unobserved occupied slots also conflict. Conflicts retain stored bytes and quarantine the competing branch with an explanatory error. Failed transactions do not advance the expected token. Valid import and recovery are explicit replacement operations that back up replaced bytes. Tests cover shared-driver competing sessions, equal-turn divergent contents, unseen slots, repeat conflicts, recovery, and failed transaction retry. The authored production browser scenario now also checks two real pages sharing IndexedDB; Hermes runs that scenario.
- **RULES-003:** Existing turn, XP, credits, debt and inventory bounds preserved. Purchases, ground collection (including `take all`) and scrounging now refuse full stacks before consuming currency, loot or one-time rewards. Tests verify accepted boundary saves remain serializable, rejected actions preserve the entire state, and no partial ground collection occurs. Automatic kill rewards retain the existing inventory saturation behavior.
- **RULES-004:** Fixed. Inspect details use `Object.hasOwn`; inherited keys produce the ordinary unknown-target string and do not advance the world. Explicit inherited-key regressions pass.
- **Coverage suggestion:** Existing tests already traverse all 60 origin/class/pump-ending routes and the sustained level-ten regional quest campaign using real movement/combat and encode/decode checkpoints. Existing individual ability-effect, tier and cost tests are retained.

Evidence: `evidence/12-followups-RED.txt` records eight failing regressions before implementation; `evidence/12-followups-GREEN.txt` records the final suite. `evidence/13-atlas-audio-RED.txt` records the audio mapping regression before integration. CLI checks are not a claim of real-browser QA. See `../BROWSER_RUNTIME_QA.md` for Hermes's prior execution and `IMPLEMENTATION.md` for this follow-up.
